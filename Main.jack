/** Main class that starts the Aim and Claim game */
class Main {
    /** Initializes the game and starts it */
    function void main() {
        var Game game;
        let game = Game.new();
        do game.run();
        do game.dispose();
        return;
    }
}
